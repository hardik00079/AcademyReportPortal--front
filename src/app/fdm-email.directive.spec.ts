import { FdmEmailDirective } from './fdm-email.directive';

describe('FdmEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new FdmEmailDirective();
    expect(directive).toBeTruthy();
  });
});
